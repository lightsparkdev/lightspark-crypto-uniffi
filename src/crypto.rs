
pub fn sign_ecdsa(msg: Vec<u8>, private_key_bytes: Vec<u8>) -> Result<Vec<u8>, ()> {
    Err(())
}

pub fn verify_ecdsa(msg: Vec<u8>, signature_bytes: Vec<u8>, public_key_bytes: Vec<u8>) -> Result<bool, ()> {
    Err(())
}

pub fn encrypt_ecies(msg: Vec<u8>, public_key_bytes: Vec<u8>) -> Result<Vec<u8>, ()> {
    Err(())
}

pub fn decrypt_ecies(msg: Vec<u8>, private_key_bytes: Vec<u8>) -> Result<Vec<u8>, ()> {
    Err(())
}